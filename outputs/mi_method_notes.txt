Predictive mean matching is implemented with iterative Bayesian-ridge prediction and donor matching (k=20, five update cycles per imputation). statsmodels MICEData(k_pmm=20) was evaluated but was too slow for the full MAR/MNAR sensitivity grid in this local pipeline. Raw GDP is not imputed independently; GDP per capita is derived from the imputed log-GDP value for originally missing raw GDP cells.
